# pyproject.toml (Corrected Version)

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "jobhunt-gpt"
version = "0.1.0"
dynamic = ["version", "requires-python"]

[tool.setuptools.packages.find]
# This section tells setuptools to automatically find all your packages 
# (like api, analyzer, etc.) so they can be imported correctly.
